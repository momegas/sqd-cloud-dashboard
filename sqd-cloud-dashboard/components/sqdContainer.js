import React from "react";
import { Container } from "@mui/material";

export default function SqdContainer() {
  return (
    <Container
      sx={{
        backgroundColor: "#3A557F",
        width: "90%",
        borderRadius: "12px",
        minHeight: "100px",
        margin: "0 auto",
      }}
    ></Container>
  );
}
